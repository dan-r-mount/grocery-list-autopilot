# Optional runtime config only — never put Sainsbury's passwords or session cookies here.
# Retailer sessions: encrypted vault via the Pixel Connect UI (data/sainsburys.vault).
# Notification topic: set in the web UI (saved to data/settings.json), not required here.

SAINSBURYS_WRITE_ENABLED=false
KEEP_SYNC_ENABLED=false

PORT=3001
HOST=0.0.0.0
WEB_ORIGIN=http://localhost:3000
